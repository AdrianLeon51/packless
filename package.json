{
  "name": "packless-landing",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply packless-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply packless-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
